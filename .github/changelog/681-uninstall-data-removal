Significance: minor
Type: added

Add an Uninstall settings screen that controls what deleting the plugin removes. Every option is off by default, so deleting GatherPress keeps your data unless you ask for it to go. You can opt in separately to removing events, venues, RSVPs, topics, generated files, scheduled jobs, per-user preferences, and the plugin settings.
