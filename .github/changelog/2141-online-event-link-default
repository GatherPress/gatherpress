Significance: patch
Type: changed

The online event link's default text is built when the block renders instead of being written into the post, so its wording can change without editing anyone's content.
