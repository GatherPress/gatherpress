name: PHPUnit Tests
on:
  push:
    branches:
      - main
      - develop
  pull_request:

jobs:
  test-php:
    name: ${{ matrix.php_versions }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ ubuntu-latest ]
        php_versions: [ '8.0', '7.4', '7.3' ]
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php_versions }}
          coverage: xdebug

      - name: Composer Install
        run: composer install --optimize-autoloader --prefer-dist

      - name: Install NodeJS
        uses: actions/setup-node@v2
        with:
          node-version-file: '.nvmrc'

      - name: Log debug information
        run: |
          npm --version
          node --version
          git --version
          php --version
          composer --version

      - name: Running PHPUnit Tests
        run: |
          npm i -g @wordpress/env
          npm run test:unit:php
          containers=$(docker container ls | awk 'NR > 1 {print $1}')
          for container in $containers; do docker cp $container:/var/www/html/coverage.xml coverage.xml && echo 'coverage.xml file moved.'; done
#          for container in containers; do test -d $container:$(docker exec $container pwd)/coverage.xml && echo 'It Exists'; done
#          for container in containers; do docker cp $container:$(docker exec $container pwd)/coverage.xml coverage.xml; done

#          echo $(docker container ls)
#          docker cp $(docker ps -aqf "name=tests-wordpress"):$(docker exec $(docker ps -aqf "name=tests-wordpress") pwd)/coverage.xml coverage.xml
        if: ${{ success() || failure() }}

      - name: Fix Code Coverage Paths
        run: sed -i 's@'$GITHUB_WORKSPACE'@/github/workspace/@g' coverage.xml

      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
